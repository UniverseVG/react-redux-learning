// import { productsList } from './productsList'

// export default function productsReducer(state = productsList) {
//   return state
// }

import { productsList } from "./productsList";

export default function productReducers(state = productsList) {
  return state;
}
